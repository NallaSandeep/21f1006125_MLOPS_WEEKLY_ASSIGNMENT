apiVersion: v1
kind: Service
metadata:
  name: iris-prediction-service
spec:
  selector:
    app: iris-prediction-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8200
  type: LoadBalancer